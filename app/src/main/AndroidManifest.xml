<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infosecurity.mac_celllocator" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".map"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.infosecurity.mac_celllocator.MAC"
            android:label="@string/title_activity_mac" >
        </activity>
        <activity
            android:name="com.infosecurity.mac_celllocator.triangle"
            android:label="@string/title_activity_triangle" >
        </activity>
        <activity
            android:name="com.infosecurity.mac_celllocator.RegisterAccessPointLocation"
            android:label="RegisterAccessPointLocation">
        </activity>
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>

</manifest>
